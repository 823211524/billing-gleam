import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const GenerateBills = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Generate Bills</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Bill generation interface will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default GenerateBills;